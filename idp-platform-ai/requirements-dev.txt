# ── Dev + CI dependencies ─────────────────────────────────────
-r requirements.txt

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2

# Linting (pipeline stage 1)
flake8==7.1.1
flake8-bugbear==24.8.19

# Security scanning (pipeline stage 2 — bandit)
bandit==1.7.10

# Type checking
mypy==1.11.2
